Abstract Class:
- They both store data. Dictionary<string, string>
- Data gets written to the discs by using a laser.
- The data on the discs can be read by using a laser.
- Once loaded, they both report information like name, capacity, contents, and disc type.
- The data on the discs can be read by using a laser.
- Overwrite data already on the disc.

CD/DVD/FloppyDisc Classes:
() - 

Interface:
- const int for each SpinSpeed
- They both spin.
- They both need to be stopped!!
- They can both be ejected from their slot.
